Later, when your friend completes the backend

You only change one line:

Current:

return MockAuthService();

Later:

return RealAuthService(ApiClient.dio);

Everything else in your app continues to work.

